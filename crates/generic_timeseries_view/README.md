# Generic timeseries view

This library implements a custom view for entities with multiple Scalar components.

It builds on (and mostly reuses) the MIT-licensed Rerun re_timeseries_view code, so the license is reproduced in the source code here.